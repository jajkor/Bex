(bit P True)
(bit Q 0)

(bit_vector N 0b1010)
(bit_vector M 0b0101)

(circuit HALF_ADDER(A B)
  (return (xor A B))
  (return (and A B))
)

(print (or P Q))
(print (HALF_ADDER P Q))

(print N)
(print M)
